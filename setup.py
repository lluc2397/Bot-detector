from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project to detect if a visiteur is a bot or an actual person',
    author='Lucas Montes',
    license='MIT',
)
